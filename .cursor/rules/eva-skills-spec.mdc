---
description: E.V.A. Skills spec v0.1 — FSM, guards, skill runs
globs: E.V.A./**/skills/**,E.V.A./backend/**/skill*.py,E.V.A./backend/runtime/fsm_engine.py,E.V.A./backend/runtime/skill_runner.py,E.V.A./backend/runtime/guards.py
alwaysApply: false
---

# Skills Spec

Read `.cursor/skills/eva-specs/skills.md` (full: `E.V.A./docs/SKILLS_SPEC_v0.1.md`).

- Skill → FSM → Action id → Tool command (never call tools from FSM)
- Handlers: `on.<event>` with optional `guard`, `actions`, `to`
- Guards: `result.*`, `event.*`, `payload.*`, `vars.*`; comparisons + `.length`
- Domain events (`channel.message.received`) ≠ action results (`action.<id>.completed`)
- `params` (inputs) vs `vars` (runtime state); waiting = Skill Run lifecycle
