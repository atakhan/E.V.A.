# Сводка ключей, которые Infisical env=staging должен отдать compose.
# Не копировать на сервер: папки /eva-postgres, /eva-redis, /eva-backend
# (шаблоны — infisical/stage/*.env.example, загрузка — make stage-secrets-upload).

# --- PostgreSQL (eva-postgres-stage) ---
POSTGRES_USER=eva
POSTGRES_DB=eva
POSTGRES_PASSWORD=change_me_strong_password
DATABASE_URL=postgresql+psycopg2://eva:change_me_strong_password@eva-postgres-stage:5432/eva

# --- Redis (eva-redis-stage) ---
REDIS_URL=redis://eva-redis-stage:6379/0

# Fernet key: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
EVA_CREDENTIALS_KEY=

# Public URLs behind nginx_proxy
EVA_PUBLIC_URL=https://eva-stage.tetrakom-crm-miniapp.ru
EVA_WEBHOOK_BASE=https://eva-stage.tetrakom-crm-miniapp.ru

EVA_TELEGRAM_MODE=real
EVA_TELEGRAM_POLLING=false
EVA_POLZA_MODE=real
POLZA_API_BASE=https://polza.ai/api/v1
POLZA_DEFAULT_MODEL=openai/gpt-4o-mini

EVA_WEB_CLIENT_DOCKER_REWRITE=false
EVA_AUTO_MIGRATE=true
EVA_PRUNE_EPHEMERAL_AGENTS=false
